public enum SortingOrder { Ascending = 0, Descending = 1 }
public enum TopicType { Void = 0, Func = 1 }
public enum FileName { PlayerData = 0 }

public enum SceneName
{
    Authentication = 0,
    MainMenu = 1
}

public enum SaveType
{
    JsonEncrypted = 0,
    JsonDecrypted = 1,
    Binary = 2,
    Xml = 3,
    TextEncrypted = 4,
    TextDecrypted = 5
}